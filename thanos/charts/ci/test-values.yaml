thanos:
  objectStoreSecretVolume:
    - name: objectstore-secret
      secret:
        defaultMode: 420
        secretName: thanos-metrics-objectstore

prometheus-operator:
  prometheusOperator:
    enabled: true
  crds:
    enabled: true

testFramework:
  ci:
    enabled: true
