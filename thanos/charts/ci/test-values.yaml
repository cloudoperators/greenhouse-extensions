prometheus-operator:
  defaultRules:
    create: true
    labels:
      thanos-ruler: thanos
  prometheusOperator:
    enabled: true
  crds:
    enabled: true

ci:
  enabled: true

thanos:
  ruler:
    matchLabel: thanos
    alertmanagers:
      authentication:
        enabled: true
        ssoCert:
        ssoKey:
      hosts:
