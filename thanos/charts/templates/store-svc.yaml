{{ if and .Values.store.enabled (not .Values.query.standalone) }}
apiVersion: v1
kind: Service
metadata:
  {{- if .Values.store.annotations }}
  annotations:
    {{ toYaml .Values.store.annotations | nindent 8 }}
  {{- end }}
  labels:
    {{- include "plugin.labels" . | nindent 4 }}
    {{- include "thanos.labels" . | nindent 4 }}
    {{- if .Values.store.serviceLabels }}
    {{ toYaml .Values.store.serviceLabels | nindent 4 }}
    {{- end }}
  name: {{ include "release.name" . }}-store
spec:
  ports:
  - name: grpc
    port: 10901
    protocol: TCP
    targetPort: grpc
  - name: http
    port: 10902
    protocol: TCP
    targetPort: http
  selector:
    app.kubernetes.io/managed-by: {{ include "release.name" . }}
    app.kubernetes.io/name: store
{{ end }}
