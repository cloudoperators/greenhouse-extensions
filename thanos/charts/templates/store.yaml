{{ if and .Values.store.enabled (not .Values.query.standalone) }}
apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
  labels:
    {{- include "plugin.labels" . | nindent 4 }}
    {{- include "thanos.labels" . | nindent 4 }}
    {{- if .Values.store.deploymentLabels }}
    {{ toYaml .Values.store.deploymentLabels | nindent 4 }}
    {{- end }}
  name: {{ include "release.name" . }}-store
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/managed-by: {{ include "release.name" . }}
      app.kubernetes.io/name: store
  strategy:
    rollingUpdate:
      maxSurge: 25%
      maxUnavailable: 25%
    type: RollingUpdate
  template:
    metadata:
      {{- if .Values.store.annotations }}
      annotations:
        {{ toYaml .Values.store.annotations | nindent 8 }}
      {{- end }}
      labels:
        app.kubernetes.io/managed-by: {{ include "release.name" . }}
        app.kubernetes.io/name: store
        {{- if .Values.store.containerLabels }}
        {{ toYaml .Values.store.containerLabels | nindent 8 }}
        {{- end }}
      name: {{ include "release.name" . }}-store
    spec:
      containers:
      - args:
        - store
        - --objstore.config-file=/etc/config/{{ include "thanos.objectStoreConfigFile" . }}
        - --grpc-address={{ default "0.0.0.0:10901" .Values.grpcAddress }}
        - --http-address={{ default "0.0.0.0:10902" .Values.httpAddress }}
        - --log.level={{ default "info" .Values.store.logLevel }}
        - --index-cache-size={{ default "1GB" .Values.store.indexCacheSize }}
        - --chunk-pool-size={{ default "4GB" .Values.store.chunkPoolSize }}
        {{- range .Values.store.additionalArgs }}
        - {{ . }}
        {{- end }}
        image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
        imagePullPolicy: {{ default "IfNotPresent" .Values.image.pullPolicy }}
        livenessProbe:
          failureThreshold: 2
          httpGet:
            path: /-/healthy
            port: 10902
            scheme: HTTP
          initialDelaySeconds: 5
          periodSeconds: 30
          successThreshold: 1
          timeoutSeconds: 5
        name: store
        ports:
        - containerPort: 10902
          name: http
          protocol: TCP
        - containerPort: 10901
          name: grpc
          protocol: TCP
        readinessProbe:
          failureThreshold: 2
          httpGet:
            path: /-/ready
            port: 10902
            scheme: HTTP
          initialDelaySeconds: 5
          periodSeconds: 30
          successThreshold: 1
          timeoutSeconds: 5
        volumeMounts:
        - mountPath: /etc/config/
          name: objectstore-secret
          readOnly: true
        - mountPath: /data
          name: data
      initContainers:
      - name: init-permissions
        {{- $registry := .Values.global.imageRegistry | default .Values.initChownData.image.registry -}}
        {{- if .Values.initChownData.image.sha }}
        image: "{{ $registry }}/{{ .Values.initChownData.image.repository }}:{{ .Values.initChownData.image.tag }}@sha256:{{ .Values.initChownData.image.sha }}"
        {{- else }}
        image: "{{ $registry }}/{{ .Values.initChownData.image.repository }}:{{ .Values.initChownData.image.tag }}"
        {{- end }}
        command: ['sh', '-c', 'chown 1000:3000 /data && chmod 750 /data']
        securityContext:
          runAsUser: 0
        volumeMounts:
        - mountPath: /data
          name: data
      restartPolicy: Always
      securityContext:
        runAsUser: 1000
        runAsGroup: 3000
        fsGroup: 2000
      volumes:
      - name: objectstore-secret
        secret:
          defaultMode: 420
          secretName: {{ include "thanos.objectStoreSecretName" . }}
      - name: data
        emptyDir: {}
{{ end }}
