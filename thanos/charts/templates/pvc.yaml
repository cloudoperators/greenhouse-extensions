{{ if and .Values.compactor.enabled (not .Values.query.standalone) }}
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  {{- if .Values.compactor.annotations }}
  annotations:
    {{ toYaml .Values.compactor.annotations | nindent 8 }}
  {{- end }}
  labels:
    {{- include "plugin.labels" . | nindent 4 }}
    {{- include "thanos.labels" . | nindent 4 }}
    {{- if .Values.compactor.volume.labels }}
    {{ toYaml .Values.compactor.volume.labels | nindent 4 }}
    {{- end }}
  name: {{ include "release.name" . }}-compactor
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: {{ default "100Gi" .Values.compactor.volume.size }}
{{ end }}
