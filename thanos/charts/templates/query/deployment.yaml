# SPDX-FileCopyrightText: 2024 SAP SE or an SAP affiliate company and Greenhouse contributors
# SPDX-License-Identifier: Apache-2.0

apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    {{- include "plugin.labels" . | nindent 4 }}
    {{- include "thanos.labels" . | nindent 4 }}
    {{- if .Values.query.deploymentLabels }}
    {{ toYaml .Values.query.deploymentLabels | nindent 4 }}
    {{- end }}
  name: {{ include "release.name" . }}-query
spec:
  progressDeadlineSeconds: 600
  replicas: {{ default 1 .Values.query.replicas }}
  revisionHistoryLimit: 10
  selector:
    matchLabels:
      app.kubernetes.io/managed-by: {{ include "release.name" . }}
      app.kubernetes.io/name: query
  strategy:
    rollingUpdate:
      maxSurge: 25%
      maxUnavailable: 25%
    type: RollingUpdate
  template:
    metadata:
      {{- if .Values.query.annotations }}
      annotations:
        {{ toYaml .Values.query.annotations | nindent 8 }}
      {{- end }}
      labels:
        app.kubernetes.io/managed-by: {{ include "release.name" . }}
        app.kubernetes.io/name: query
        {{- if .Values.query.containerLabels }}
        {{ toYaml .Values.query.containerLabels | nindent 8 }}
        {{- end }}
      name: {{ include "release.name" . }}-query
    spec:
      containers:
      - args:
        - query
        - --grpc-address={{ default "0.0.0.0:10901" .Values.grpcAddress }}
        - --http-address={{ default "0.0.0.0:10902" .Values.httpAddress }}
        - --log.level={{ default "info" .Values.query.logLevel }}
        {{- if .Values.query.autoDownsampling }}
        - --query.auto-downsampling
        {{- end }}
        - --query.replica-label={{ default "prometheus_replica" .Values.query.replicaLabel }}
        - {{ default "--web.external-prefix=/thanos" .Values.query.web.externalPrefix }}
        - {{ default "--web.route-prefix=/thanos" .Values.query.web.routePrefix }}
        {{- range .Values.query.additionalArgs }}
        - {{ . }}
        {{- end }}
        - --endpoint.sd-config-file=/etc/thanos/sd-config/endpoint-targets.yaml
        {{- if or .Values.query.tls.data .Values.query.tls.secretName }}
        - --grpc-client-tls-ca=/etc/tls/client/ca.crt
        - --grpc-client-tls-cert=/etc/tls/client/tls.crt
        - --grpc-client-tls-key=/etc/tls/client/tls.key
        - --grpc-client-tls-secure
        {{- end }}
        image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
        imagePullPolicy: {{ default "IfNotPresent" .Values.image.pullPolicy }}
        livenessProbe:
          failureThreshold: 2
          httpGet:
            path: /-/healthy
            port: 10902
            scheme: HTTP
          initialDelaySeconds: 5
          periodSeconds: 30
          successThreshold: 1
          timeoutSeconds: 5
        name: query
        ports:
        - containerPort: 10902
          name: http
          protocol: TCP
        - containerPort: 10901
          name: grpc
          protocol: TCP
        readinessProbe:
          failureThreshold: 2
          httpGet:
            path: /-/ready
            port: 10902
            scheme: HTTP
          initialDelaySeconds: 5
          periodSeconds: 30
          successThreshold: 1
          timeoutSeconds: 5
        volumeMounts:
        - name: sd-config
          mountPath: /etc/thanos/sd-config
      {{- if or .Values.query.tls.data .Values.query.tls.secretName }}
        - name: tls-certificate
          mountPath: /etc/tls/client
          readOnly: true 
      {{- end }}
      restartPolicy: Always
      terminationGracePeriodSeconds: 30
      volumes:
        - name: sd-config
          configMap:
            name: {{ include "release.name" . }}-sd-config
      {{- if or .Values.query.tls.data .Values.query.tls.secretName }}
        - name: tls-certificate
          secret:
            defaultMode: 420
            secretName: {{ .Values.query.tls.secretName | default (printf "%s-tls" .Release.Name) }}
      {{- end }}


