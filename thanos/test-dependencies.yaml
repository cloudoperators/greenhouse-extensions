dependencies:
  - kube-monitoring
values:
  kubeMonitoring:
    blackboxExporter:
      enabled: true
    prometheus:
      enabled: true
      serviceMonitor:
        enabled: false
      prometheusSpec:
        thanos:
          objectStorageConfig:
            secret:
              type: FILESYSTEM
              config:
                directory: "/test"
              prefix: ""
