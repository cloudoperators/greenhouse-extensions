# SPDX-FileCopyrightText: 2024 SAP SE or an SAP affiliate company and Greenhouse contributors
# SPDX-License-Identifier: Apache-2.0

# OIDC Authentication Configuration
# Enable OpenID Connect authentication for OpenSearch and Dashboards
auth:
  oidc:
    # -- Enable OIDC authentication. When enabled, adds an OpenID Connect auth domain to OpenSearch.
    enabled: false
    # -- OpenID Connect provider URL (e.g., https://provider.example.com/.well-known/openid-configuration)
    provider: ""
    # -- Claim key to use as username from the OIDC token
    subjectKey: "name"
    # -- Claim key to use for roles from the OIDC token
    rolesKey: "roles"
    # -- Path to CA certificate for OIDC provider verification (relative to OpenSearch config dir)
    # Leave empty to use system CA bundle
    caPath: ""
    # Dashboards OIDC client configuration (creates a secret for env var injection)
    dashboards:
      # -- OIDC client ID for OpenSearch Dashboards (required when auth.oidc.enabled is true)
      clientId: ""
      # -- OIDC client secret for OpenSearch Dashboards (required when auth.oidc.enabled is true)
      clientSecret: ""
      # -- OIDC scopes to request
      scope: "openid email profile"
      # -- Base redirect URL for OIDC callback (your dashboards URL, e.g., https://dashboards.example.com/)
      baseRedirectUrl: ""

serviceProxy:
  dashboards:
    # -- Expose the OpenSearch Dashboards UI through the Greenhouse service-proxy.
    # When enabled, an extra `<cluster.cluster.name>-dashboards-ui` Service is rendered
    # (falling back to `<release>-dashboards-ui` when `cluster.cluster.name` is not set)
    # with the `greenhouse.sap/expose: "true"` annotation that the service-proxy watches.
    enabled: true

s3:
  # -- Enable the S3 keystore Secret. When enabled, renders a Secret with
  # `s3.client.<name>.access_key` / `s3.client.<name>.secret_key` entries for
  # the repository-s3 plugin (warm nodes, snapshots). Wire the rendered Secret
  # via `cluster.cluster.general.keystore` and configure endpoints under
  # `cluster.cluster.general.additionalConfig`.
  enabled: false

  # -- Name of the rendered Secret.
  secretName: "opensearch-s3-keystore"

  # -- Map of S3 client names to credentials.
  # @default -- <pre>clients: {}</pre>
  clients: {}
  #  default:
  #    accessKey: ""
  #    secretKey: ""
  #  ceph:
  #    accessKey: ""
  #    secretKey: ""

certManager:
  # -- Enable cert-manager integration for issuing TLS certificates
  enable: true

  issuer:
    # -- API group for the DigicertIssuer custom resource
    digicert: {}

    # -- Name of the CA Issuer to be used for internal certs
    ca:
      name: opensearch-ca-issuer

    # -- Name of the self-signed issuer used to sign the internal CA certificate
    selfSigned:
      name: opensearch-issuer

  defaults:
    privateKey:
      # -- Algorithm used for generating private keys
      algorithm: RSA
      # -- Encoding format for private keys (PKCS8 recommended)
      encoding: PKCS8
      # -- Key size in bits for RSA keys
      size: 2048

    durations:
      # -- Validity period for CA certificates (1 year)
      ca: 8760h
      # -- Validity period for leaf certificates (200 days to comply with CA/B Forum baseline requirements)
      leaf: 4800h

    # -- List of extended key usages for certificates
    usages:
      - digital signature
      - key encipherment
      - server auth
      - client auth

  # -- Override HTTP DNS names for OpenSearch client endpoints
  httpDnsNames:
    - opensearch-client.tld

  # -- Override DNS names for OpenSearch Dashboards endpoints (used for dashboards ingress certificate)
  dashboardsDnsNames:
    - opensearch-dashboards.tld

operator:
  # -- Install the OpenSearch operator subchart with its
  # ServiceMonitor and PrometheusRule alerts. Set false on additional
  # releases that reuse the operator from another release. Note: CRDs
  # in chart/crds/ are managed by Helm independently of this flag.
  enabled: true
  namespace: ""
  nameOverride: ""
  fullnameOverride: ""

  podAnnotations: {}
  podLabels: {}
  nodeSelector: {}
  tolerations: []
  securityContext:
    runAsNonRoot: true
  priorityClassName: ""
  manager:
    # By default the operator binds its metrics endpoint to the loopback interface.
    # To expose metrics, set this to ":8443". When exposing metrics, Prometheus may need ClusterRole/ClusterRoleBinding
    # to authenticate/authorize against the /metrics endpoint
    metricsBindAddress: "127.0.0.1:8080"
    securityContext:
      allowPrivilegeEscalation: false
    extraEnv: []
    resources:
      limits:
        cpu: 200m
        memory: 500Mi
      requests:
        cpu: 100m
        memory: 350Mi

    livenessProbe:
      failureThreshold: 3
      httpGet:
        path: /healthz
        port: 8081
      periodSeconds: 15
      successThreshold: 1
      timeoutSeconds: 3
      initialDelaySeconds: 10

    readinessProbe:
      failureThreshold: 3
      httpGet:
        path: /readyz
        port: 8081
      periodSeconds: 15
      successThreshold: 1
      timeoutSeconds: 3
      initialDelaySeconds: 10

    # Set this to false to disable the experimental parallel recovery in case you are experiencing problems
    parallelRecoveryEnabled: true
    # Set this to true to enable the standard go pprof endpoints on port 6060 (https://pkg.go.dev/net/http/pprof)
    # Should only be used for debugging purposes
    pprofEndpointsEnabled: false

    image:
      repository: opensearchproject/opensearch-operator
      ## tag default uses appVersion from Chart.yaml, to override specify tag tag: "v1.1"
      tag: ""
      pullPolicy: "Always"

    ## Optional array of imagePullSecrets containing private registry credentials
    imagePullSecrets: []
    # - name: secretName

    dnsBase: cluster.local

    # Log level of the operator. Possible values: debug, info, warn, error
    loglevel: debug

    # If a watchNamespace is specified, the manager's cache will be restricted to
    # watch objects in the desired namespace. Defaults is to watch all namespaces.
    watchNamespace:

  # Install the Custom Resource Definitions with Helm
  installCRDs: false

  serviceAccount:
    # Specifies whether a service account should be created
    create: true
    # Override the service account name. Defaults to opensearch-operator-controller-manager
    name: "opensearch-operator-controller-manager"

  # Webhook configuration. failurePolicy is set to Ignore so initial install
  # succeeds even when the operator pod is not yet ready to handle admission.
  webhook:
    enabled: true
    failurePolicy: Ignore

  ## If this is set to true, RoleBindings will be used instead of ClusterRoleBindings, inorder to restrict ClusterRoles
  ## to the namespace where the operator and OpenSearch cluster are in. In that case, specify the namespace where they
  ## are in in manager.watchNamespace field.
  ## If false, ClusterRoleBindings will be used
  useRoleBindings: false

# -- Additional labels for PrometheusRule alerts
additionalRuleLabels: {}

cluster:
  nameOverride: ""
  fullnameOverride: ""

  serviceAccount:
    # -- Create Service Account
    create: false

    # -- Service Account name. Set `general.serviceAccount` to use this Service Account for the Opensearch cluster
    name: ""

    # -- Service Account annotations
    annotations: {}

  # OpenSearchCluster configuration
  cluster:
    # -- OpenSearchCluster name, by default release name is used
    name: "opensearch-logs"

    # -- OpenSearchCluster annotations
    annotations: {}

    # -- OpenSearchCluster labels
    labels: {}

    # OpenSearchCluster general configuration
    general:
      # -- Extra items to add to the opensearch.yml.
      additionalConfig:
        plugins.security.ssl_cert_reload_enabled: "true"

      # -- Additional volumes to mount to all pods in the cluster. Supported volume types configMap, emptyDir, secret (with default Kubernetes configuration schema)
      additionalVolumes: []
        # - name: "volume-name"
        #   path: "/tmp/volume"
        ## Whether to restart the pods on content change
        #   restartPods: false

      # -- Controls whether to drain data notes on rolling restart operations
      drainDataNodes: false

      # -- Opensearch service http port
      httpPort: 9200

      # -- Opensearch image
      image: docker.io/opensearchproject/opensearch

      # -- Default image pull policy
      imagePullPolicy: IfNotPresent

      # -- Default image pull secret
      # imagePullSecrets: []

      # -- Populate opensearch keystore before startup
      keystore: []
  #      - secret:
  #          name: secret-name
  #      - secret:
  #          name: many-secret-values
  #        keyMappings:
  #          # Only read "sensitive-value" from the secret, keep its name.
  #          sensitive-value: sensitive-value

      # Monitoring configuration. Check [documentation](https://github.com/Opster/opensearch-k8s-operator/blob/main/docs/userguide/main.md#adding-opensearch-monitoring-to-your-cluster) how to configure it.
      monitoring:
        # -- Enable cluster monitoring
        enable: false

        # -- Secret with 'username' and 'password' keys for monitoring user. You could also use OpenSearchUser CRD instead of setting it.
        monitoringUserSecret: ""

        # -- Custom URL for the monitoring plugin
        pluginUrl: "https://github.com/opensearch-project/opensearch-prometheus-exporter/releases/download/3.7.0.0/prometheus-exporter-3.7.0.0.zip"

        # -- How often to scrape metrics
        scrapeInterval: 30s

        # -- Override the tlsConfig of the generated ServiceMonitor
        tlsConfig:
          insecureSkipVerify: true

        # -- ServiceMonitor labels
        labels: {}

      # -- List of Opensearch plugins to install
      pluginsList: []

      # -- Opensearch pod security context configuration
      podSecurityContext: {}

      # -- Opensearch securityContext
      securityContext: {}

      # -- Opensearch serviceAccount name. If Service Account doesn't exist it could be created by setting `serviceAccount.create` and `serviceAccount.name`
      serviceAccount: ""

      # -- Opensearch service name
      serviceName: ""

      # -- Enable setVMMaxMapCount. OpenSearch requires the Linux kernel vm.max_map_count option to be set to at least 262144
      setVMMaxMapCount: true

      # -- Opensearch snapshot repositories configuration
      snapshotRepositories: []
  #      - name: "snapshot-repo"
  #        type: s3
  #        settings:
  #          bucket: opensearch-s3-snapshot
  #          region: us-east-1
  #          base_path: os-snapshot

      vendor: Opensearch

      # -- Opensearch version
      version: 3.7.0

    # OpenSearchCluster boostrap pod configuration
    bootstrap:
      # -- bootstrap pod affinity rules
      affinity: {}

      # -- bootstrap pod jvm options. If jvm is not provided then the java heap size will be set to half of resources.requests.memory which is the recommended value for data nodes.
      # If jvm is not provided and resources.requests.memory does not exist then value will be -Xmx512M -Xms512M
      jvm: ""

      # -- bootstrap pod node selectors
      nodeSelector: {}

      # -- bootstrap pod cpu and memory resources
      resources: {}

      # -- bootstrap pod tolerations
      tolerations: []

    # OpenSearchCluster additional services
    confMgmt:
      # -- Enable nodes to be safely removed from the cluster
      smartScaler: true

    # OpenSearchCluster dashboards configuration
    dashboards:
      # Example proxy auth config:
      #   opensearch.requestHeadersAllowlist: '["securitytenant","Authorization","x-forwarded-for","x-forwarded-user","x-forwarded-groups","x-forwarded-email"]'
      #   opensearch_security.auth.type: "proxy"
      #   opensearch_security.proxycache.user_header: "x-forwarded-user"
      #   opensearch_security.proxycache.roles_header: "x-forwarded-groups"
      #
      # Example OIDC auth config (when auth.oidc.enabled=true):
      #   opensearch.requestHeadersAllowlist: '["Authorization", "security_tenant"]'
      #   opensearch_security.auth.type: '["openid"]'
      #   opensearch_security.openid.connect_url: "https://provider.example.com/.well-known/openid-configuration"
      #   opensearch_security.openid.client_id: "${OIDC_CLIENT_ID}"
      #   opensearch_security.openid.client_secret: "${OIDC_CLIENT_SECRET}"
      #   opensearch_security.openid.scope: "openid email profile"
      #   opensearch_security.openid.base_redirect_url: "https://dashboards.example.com/"
      # -- Additional properties for opensearch_dashboards.yaml. Configure auth (proxy or OIDC) via plugin preset.
      additionalConfig: {}

      # -- dashboards pod affinity rules
      affinity: {}

      # -- dashboards annotations
      annotations: {}

      # -- dashboards Base Path for Opensearch Clusters running behind a reverse proxy
      basePath: ""

      # -- Enable dashboards deployment
      enable: true

      # When using OIDC, add environment variables for OIDC credentials:
      # env:
      #   - name: OIDC_CLIENT_ID
      #     valueFrom:
      #       secretKeyRef:
      #         name: opensearch-dashboards-oidc
      #         key: client_id
      #   - name: OIDC_CLIENT_SECRET
      #     valueFrom:
      #       secretKeyRef:
      #         name: opensearch-dashboards-oidc
      #         key: client_secret
      # -- dashboards pod env variables
      env: []

      # -- dashboards image
      image: docker.io/opensearchproject/opensearch-dashboards

      # -- dashboards image pull policy
      imagePullPolicy: "IfNotPresent"

      # -- dashboards image pull secrets
      imagePullSecrets: []

      # -- dashboards labels
      labels: {}

      # -- dashboards pod node selectors
      nodeSelector: {}

      # -- Secret that contains fields username and password for dashboards to use to login to opensearch, must only be supplied
      # if a custom securityconfig is provided
      opensearchCredentialsSecret:
        name: "dashboards-credentials"

      # -- List of dashboards plugins to install
      pluginsList: []

      # -- dasboards pod security context configuration
      podSecurityContext: {}

      # -- number of dashboards replicas
      replicas: 1

      # -- dashboards pod cpu and memory resources
      resources: {}

      # -- dashboards security context configuration
      securityContext: {}

      service:
        # -- source ranges for a loadbalancer
        loadBalancerSourceRanges: []
        # -- dashboards service type
        type: ClusterIP
        # -- dashboards service metadata labels
        labels: {}

      tls:
        # -- Secret that contains the ca certificate as ca.crt. If this and generate=true is set the existing
        # CA cert from that secret is used to generate the node certs. In this case must contain ca.crt and ca.key fields
        caSecret:
          name: "opensearch-ca-cert"

        # -- Enable HTTPS for dashboards
        enable: false

        # -- generate certificate, if false secret must be provided
        generate: false

        # -- Optional, name of a TLS secret that contains ca.crt, tls.key and tls.crt data. If ca.crt is in a different
        # secret provide it via the caSecret field
        secret:
          name: "opensearch-http-cert"

      # -- dashboards pod tolerations
      tolerations: []

      # -- dashboards version
      version: 3.7.0

    # initHelper configuration
    initHelper:
      # -- initHelper image repository for the init container used to set vm.max_map_count. Override for registry mirrors.
      image: docker.io/busybox

      # -- initHelper image pull policy
      imagePullPolicy: "IfNotPresent"

      # -- initHelper image pull secret
      imagePullSecrets: []

      # -- initHelper pod cpu and memory resources
      resources: {}

      # -- initHelper version
      version: "1.36"

    # -- Opensearch nodes configuration
    # @default -- <pre>nodePools:<br>  - component: node<br>    diskSize: "30Gi"<br>    replicas: 2<br>    roles:<br>      - "cluster_manager"<br>      - "data"<br>      - "ingest"<br>    resources:<br>      requests:<br>        memory: "1Gi"<br>        cpu: "500m"<br>      limits:<br>        memory: "4Gi"<br>        cpu: 2</pre>
    nodePools:
      - component: node
        diskSize: "30Gi"
        replicas: 2
        roles:
          - "cluster_manager"
          - "data"
          - "ingest"
        resources:
          requests:
            memory: "1Gi"
            cpu: "500m"
          limits:
            memory: "4Gi"
            cpu: 2

    # Configuration for the client service used to expose client nodes externally.
    client:
      service:
        # -- Enable or disable the external client service.
        enabled: false
        # -- Kubernetes service type. Defaults to `ClusterIP`, but should be set to `LoadBalancer` to expose OpenSearch client nodes externally.
        type: ClusterIP
        # -- Annotations to add to the service, e.g. disco.
        annotations: {}
        # -- Ports to expose for the client service.
        ports:
          - name: http
            port: 9200
            targetPort: 9200
            protocol: TCP
        # -- List of allowed IP ranges for external access when service type is `LoadBalancer`.
        loadBalancerSourceRanges: []
        # -- List of external IPs to expose the service on.
        externalIPs: []

    # Opensearch security configuration
    security:
      config:
        # -- Secret that contains fields username and password to be used by the operator to access the opensearch cluster
        # for node draining. Must be set if custom securityconfig is provided.
        adminCredentialsSecret:
          name: "admin-credentials"

        # -- TLS Secret that contains a client certificate (tls.key, tls.crt, ca.crt) with admin rights in the opensearch cluster.
        # Must be set if transport certificates are provided by user and not generated
        adminSecret:
          name: "opensearch-admin-cert"

        # -- Secret that contains the different yml files of the opensearch-security config (config.yml, internal_users.yml, etc)
        securityConfigSecret:
          name: "opensearch-security-config"

      # Configure tls usage for transport and http interface
      tls:
        http:
          # -- DNs of certificates that should have admin access, mainly used for securityconfig updates via securityadmin.sh,
          # only used when existing certificates are provided
          adminDn:
            - "CN=admin"

          # -- Optional, secret that contains the ca certificate as ca.crt. If this and generate=true is set the existing
          # CA cert from that secret is used to generate the node certs. In this case must contain ca.crt and ca.key fields
          caSecret:
            name: "opensearch-http-cert"

          # -- If set to true the operator will generate a CA and certificates for the cluster to use,
          # if false - secrets with existing certificates must be supplied
          generate: false

          # -- Optional, name of a TLS secret that contains ca.crt, tls.key and tls.crt data. If ca.crt is in a
          # different secret provide it via the caSecret field
          secret:
            name: "opensearch-http-cert"

        transport:
          # -- Optional, secret that contains the ca certificate as ca.crt. If this and generate=true is set the existing
          # CA cert from that secret is used to generate the node certs. In this case must contain ca.crt and ca.key fields
          caSecret:
            name: "opensearch-ca-cert"

          # -- If set to true the operator will generate a CA and certificates for the cluster to use,
          # if false secrets with existing certificates must be supplied
          generate: false

          # --  Allowed Certificate DNs for nodes, only used when existing certificates are provided
          nodesDn:
            - "CN=opensearch-transport"

          # -- Separate certificate per node
          perNode: false

          # -- Optional, name of a TLS secret that contains ca.crt, tls.key and tls.crt data. If ca.crt is in a
          # different secret provide it via the caSecret field
          secret:
            name: "opensearch-transport-cert"

    # Opensearch Ingress configuration
    ingress:
      opensearch:
        # -- Enable ingress for Opensearch service
        enabled: false

        # -- Opensearch ingress annotations
        annotations: {}

        # -- Opensearch Ingress class name
        className: ""

        # -- Opensearch Ingress hostnames
        hosts: []
  #        - host: example.com
  #          paths:
  #            - path: /
  #              pathType: ImplementationSpecific

        # -- Opensearch tls configuration
        tls: []
          # - hosts:
          #   - example.com
          #  secretName: tls-secret

      # Dashboards Ingress configuration
      dashboards:
        # -- Enable ingress for dashboards service
        enabled: false

        # -- dashboards ingress annotations
        annotations: {}

        # -- Ingress class name
        className: ""

        # -- Ingress hostnames
        hosts: []
  #        - host: example.com
  #          paths:
  #            - path: /
  #              pathType: ImplementationSpecific

        # -- Ingress tls configuration
        tls: []
        #  - hosts:
        #      - example.com
        #    secretName: tls-secret

  # -- List of OpensearchRole. Includes read and write roles for logs* indices.
  # @default -- See values.yaml
  roles:
    - name: "logs-write-role"
      clusterPermissions:
        - "cluster_monitor"
        - "cluster_composite_ops"
        - "cluster:admin/ingest/pipeline/put"
        - "cluster:admin/ingest/pipeline/get"
        - "indices:admin/template/get"
        - "cluster_manage_index_templates"
      indexPermissions:
        - indexPatterns:
            - "logs*"
          allowedActions:
            - "indices:admin/template/get"
            - "indices:admin/template/put"
            - "indices:admin/mapping/put"
            - "indices:admin/create"
            - "indices:data/write/bulk*"
            - "indices:data/write/index"
            - "indices:data/read*"
            - "indices:monitor*"
            - "indices_all"
    - name: "logs-read-role"
      clusterPermissions:
        - "read"
        - "cluster_monitor"
        - "cluster_composite_ops"
        - "cluster:monitor/nodes/stats"
        - "cluster:admin/opensearch/ql/datasources/read"
        - "cluster:monitor/task/get"
        - 'cluster:admin/opendistro/reports/definition/create'
        - 'cluster:admin/opendistro/reports/definition/update'
        - 'cluster:admin/opendistro/reports/definition/on_demand'
        - 'cluster:admin/opendistro/reports/definition/delete'
        - 'cluster:admin/opendistro/reports/definition/get'
        - 'cluster:admin/opendistro/reports/definition/list'
        - 'cluster:admin/opendistro/reports/instance/list'
        - 'cluster:admin/opendistro/reports/instance/get'
        - 'cluster:admin/opendistro/reports/menu/download'
        - 'cluster:admin/opensearch/ppl'
        - "cluster:admin/ingest/pipeline/get"
        - "indices:admin/template*"
        - "indices:data/read/scroll*"
      indexPermissions:
        - indexPatterns:
            - ".kibana*"
            - ".opensearch_dashboards*"
          allowedActions:
            - "crud"
            - "read"
            - "search"
            - "get"
            - "indices:data/read/*"
            - "indices:data/write/*"
            - "indices:admin/get"
            - "indices:admin/mappings/get"
        - indexPatterns:
            - "*"
          allowedActions:
            - "search"
            - "read"
            - "get"
            - "indices:monitor/settings/get"
            - "indices:admin/mappings/get"
            - "indices:data/read/search*"
            - "indices:admin/get"
            - "indices:data/read/get"
            - "indices:admin/aliases/get"
            - "indices:admin/mappings/fields/get*"
            - "indices:admin/resolve/index"
      tenantPermissions:
        - tenantPatterns:
            - "*"
          allowedActions:
            - "kibana_all_read"
            - "kibana_all_write"
    - name: "dashboards-role"
      clusterPermissions:
        - "cluster_monitor"
        - "cluster_composite_ops"
        - "indices:admin/template*"
        - "indices:data/read/scroll*"
      indexPermissions:
        - indexPatterns:
            - ".kibana*"
            - ".opensearch_dashboards*"
            - ".tasks"
            - ".management-beats*"
          allowedActions:
            - "indices_all"
        - indexPatterns:
            - "*"
          allowedActions:
            - "indices:admin/aliases*"
            - "indices:admin/get"
            - "indices:data/read/search*"
            - "indices:data/read/get"
            - "read"

  # -- List of OpenSearch user configurations.
  # @default -- <pre>users:<br>  - name: "logs"<br>    secretName: "logs-credentials"<br>    secretKey: "password"<br>    backendRoles: []</pre>
  users:
    - name: "logs"
      secretName: "logs-credentials"
      secretKey: "password"
      backendRoles: []
    - name: "logs2"
      secretName: "logs2-credentials"
      secretKey: "password"
      backendRoles: []
    - name: "dashboards"
      secretName: "dashboards-credentials"
      secretKey: "password"
      backendRoles: []
    - name: "dashboards2"
      secretName: "dashboards2-credentials"
      secretKey: "password"
      backendRoles: []

  # -- List of OpenSearch user credentials. These credentials are used for authenticating users with OpenSearch. See values.yaml file for a full example.
  # @default -- <pre>usersCredentials:<br>  admin:<br>    username: "admin"<br>    password: "admin"<br>    hash: ""</pre>
  usersCredentials:
    admin:
      username: ""  # Specify the username for the "admin" user. If left empty, defaults to "admin".
      password: ""  # Replace password with a secure value before deploying to production. A dummy password is set in ci/test-values.yaml for testing purposes.
      hash: ""      # Optional bcrypt hash of the password.
    logs:
      username: ""  # Specify the username for the "logs" user. If left empty, defaults to "logs".
      password: ""  # Replace password with a secure value before deploying to production. A dummy password is set in ci/test-values.yaml for testing purposes.
    logs2:
      username: ""  # Specify the username for the failover user for "logs". If left empty, defaults to "logs2".
      password: ""  # Replace password with a secure value before deploying to production. A dummy password is set in ci/test-values.yaml for testing purposes.
    dashboards:
      username: ""  # Specify the username for the "dashboards" user. If left empty, defaults to "dashboards".
      password: ""  # Replace password with a secure value before deploying to production. A dummy password is set in ci/test-values.yaml for testing purposes.
    dashboards2:
      username: ""  # Specify the username for the failover user for "dashboards". If left empty, defaults to "dashboards2".
      password: ""  # Replace password with a secure value before deploying to production. A dummy password is set in ci/test-values.yaml for testing purposes.

  # -- Allows to link any number of users, backend roles and roles with a OpensearchUserRoleBinding. Each user in the binding will be granted each role
  # @default -- <pre>usersRoleBinding:<br>  - name: "logs-write"<br>    users:<br>      - "logs"<br>      - "logs2"<br>    roles:<br>      - "logs-write-role"</pre>
  usersRoleBinding:
    - name: "logs-write"
      users:
        - "logs"
        - "logs2"
      roles:
        - "logs-write-role"
    - name: "dashboards"
      users:
        - "dashboards"
        - "dashboards2"
      roles:
        - "dashboards-role"

  # -- List of additional tenants. Check values.yaml file for examples.
  tenants: []
  #  - name: "example-tenant"
  #    description: "example-tenant"

  # -- List of OpensearchActionGroup. Check values.yaml file for examples.
  actionGroups: []
  #  - name: "example-action-group"
  #    description: "Example of action group"
  #    allowedActions:
  #      - indices:admin/aliases/get
  #      - indices:admin/aliases/exists
  #    type: index

  # -- List of OpensearchComponentTemplate.
  # @default -- See values.yaml
  componentTemplates: []

  # -- List of OpensearchIndexTemplate. Includes template for logs* data stream.
  # @default -- See values.yaml
  indexTemplates:
    - name: logs-index-template
      indexPatterns:
        - "logs*"
      templateSpec:
        settings:
          index:
            number_of_shards: 1
            number_of_replicas: 1
            refresh_interval: 1s
        mappings:
          properties:
            "@timestamp":
              type: date
            message:
              type: text
            severity:
              properties:
                number:
                  type: long
                text:
                  type: keyword
      dataStream: {}
      priority: 100

  # -- List of OpenSearchISMPolicy. Includes 7-day retention policy for logs* indices.
  # @default -- See values.yaml
  ismPolicies:
    - name: logs-rollover-policy
      defaultState: hot
      description: "Logs policy with 7-day retention and size-based rollover"
      states:
        - name: hot
          actions:
            - rollover:
                minSize: "10gb"
                minDocCount: 20000000
          transitions:
            - stateName: delete
              conditions:
                minIndexAge: "7d"
        - name: delete
          actions:
            - delete: {}
      ismTemplate:
        indexPatterns:
          - "logs*"
        priority: 100

  # -- Snapshot lifecycle. Registers snapshot repositories and installs
  # ISM/SM policies via a one-shot Job. Implemented over the raw HTTP API
  # because OpensearchISMPolicy CRD does not yet expose convert_index_to_remote
  # (tracked in opensearch-k8s-operator#1402). End-to-end native operator
  # support for searchable snapshots is tracked in
  # opensearch-k8s-operator#1184. Repositories can be any type
  # supported by OpenSearch: s3, fs, hdfs, azure, gcs.
  # @default -- disabled
  snapshotLifecycle:
    # -- Enable the snapshot lifecycle install Job.
    enabled: false
    # -- OpenSearch URL the Job calls. The default points at the in-cluster
    # service. The operator names that Service after `general.serviceName`
    # when set, otherwise after `cluster.cluster.name`, otherwise after the
    # release name.
    clusterHost: "https://{{ (.Values.cluster.cluster.general).serviceName | default .Values.cluster.cluster.name | default .Release.Name }}.{{ .Release.Namespace }}.svc.cluster.local:9200"
    # -- Credentials secret. Provide a dedicated user with snapshot+ISM
    # permissions; using the cluster admin is discouraged.
    credentialsSecret:
      name: snapshot-lifecycle-credentials
      usernameKey: username
      passwordKey: password
    # -- TLS verification. Default verifies against `caSecret`. Set
    # `tlsSkipVerify: true` to disable verification entirely (not recommended).
    tlsSkipVerify: false
    # -- Secret containing the CA bundle to verify the cluster's HTTP cert.
    # Defaults to the chart's HTTP TLS secret which contains `ca.crt`.
    caSecret:
      name: opensearch-http-cert
      key: ca.crt
    # -- Install Job image. Must include Python 3.11+ (the scripts use the
    # standard library only, no `requests` dependency).
    image:
      repository: docker.io/library/python
      tag: "3.12-slim"
      pullPolicy: IfNotPresent
    # -- Optional resource requests/limits for the install Job pod.
    resources: {}
    # -- Optional node selector for the install Job pod.
    nodeSelector: {}
    # -- Optional tolerations for the install Job pod.
    tolerations: []
    # -- Pod-level securityContext for the install Job and CronJob pods.
    podSecurityContext:
      runAsNonRoot: true
      runAsUser: 1000
    # -- Job backoff limit.
    backoffLimit: 6
    # -- Seconds to keep finished install Job pods before they are garbage
    # collected. The Job name embeds a hash of all snapshotLifecycle inputs,
    # so each upgrade creates a new Job; old completed ones expire here.
    ttlSecondsAfterFinished: 86400
    # -- Override the install Job's base name (default `opensearch-snapshot-lifecycle`).
    # The rendered name is `<jobName>-<configHash>` so each upgrade produces
    # a fresh Job (Job pod templates are immutable).
    jobName: ""
    # -- Override the ConfigMap name (default `opensearch-snapshot-lifecycle`).
    configMapName: ""
    # -- CronJob that attaches the `remote-{name}-ism` policy to converted
    # remote searchable indexes. ISM templates do not auto-attach to indexes
    # created by `convert_index_to_remote`
    # (opensearch-project/index-management#1389), so this job covers the gap
    # and is a no-op when policies are already attached.
    attachRemote:
      enabled: true
      name: ""
      schedule: "30 0,12 * * *"
      backoffLimit: 3
    # -- Snapshot repositories. Each entry is registered once via
    # PUT /_snapshot/{name}. Streams reference these by name and may share a
    # repository.
    repositories: []
    # - name: my-snapshots
    #   # Any OpenSearch repository type: s3, fs, hdfs, azure, gcs.
    #   type: s3
    #   # Settings are passed verbatim to PUT /_snapshot/{name}.
    #   settings:
    #     bucket: my-snapshots
    #     client: default

    # -- List of streams to manage. Each entry produces ds-{name}-ism,
    # remote-{name}-ism, and snapshot-{name}-delete-policy.
    streams: []
    # - name: my-stream
    #   # References repositories[].name above.
    #   repository: my-snapshots
    #   indexPatterns:
    #     hot: ".ds-my-stream-*"
    #     # The retention policy targets remote indexes by glob. The default
    #     # rename_pattern restores them as `remote_{name}_<original>`, so the
    #     # matching pattern is `remote_{name}_*`.
    #     remote: "remote_my-stream_*"
    #   # Optional override for the convert_index_to_remote rename_pattern.
    #   # Default is `remote_{name}_$1`.
    #   # renamePattern: "remote_my-stream_$1"
    #   minSize: "50gb"
    #   retention:
    #     local: "7d"
    #     remote: "31d"
    #     snapshot: "93d"

  # -- Bootstrap OpenSearch Dashboards saved objects (index patterns,
  # dashboards, visualizations). Runs as a post-install/upgrade Helm
  # hook Job that POSTs to the Dashboards API.
  # @default -- disabled
  savedObjects:
    # -- Enable the saved-objects bootstrap Job.
    enabled: false
    # -- Dashboards URL. Defaults to the in-cluster HTTP Service.
    dashboardsHost: ""
    # -- Credentials Secret for a user that can write `.kibana*`.
    credentialsSecret:
      name: dashboards-credentials
      usernameKey: username
      passwordKey: password
    # -- Job image. Needs `curl` and a POSIX `sh`.
    image:
      repository: docker.io/curlimages/curl
      tag: "8.10.1"
      pullPolicy: IfNotPresent
    # -- Job pod resources.
    resources: {}
    # -- Job pod nodeSelector.
    nodeSelector: {}
    # -- Job pod tolerations.
    tolerations: []
    # -- Pod-level securityContext.
    podSecurityContext:
      runAsNonRoot: true
      runAsUser: 65534
    # -- Job backoff limit.
    backoffLimit: 6
    # -- Seconds to wait for Dashboards readiness before failing.
    waitTimeoutSeconds: 300
    # -- Seconds to keep finished Job pods before garbage collection.
    ttlSecondsAfterFinished: 86400
    # -- Override Job name (default `opensearch-saved-objects`).
    jobName: ""
    # -- Override ConfigMap name (default `opensearch-saved-objects`).
    configMapName: ""
    # -- Index patterns to upsert by id.
    indexPatterns: []
    # - id: logs
    #   title: "logs"
    #   timeFieldName: "@timestamp"
    # -- NDJSON saved-object bundles to import. Each entry references a
    # key in an existing ConfigMap (created out of band).
    imports: []
    # - configMap: my-dashboards
    #   key: kvm-overview.ndjson


# -- Extra Kubernetes manifests to deploy alongside the chart.
# Each entry can be a raw YAML string or a map object.
extraManifests: []

# Query Exporter - Prometheus exporter for custom OpenSearch queries
queryExporter:
  # -- Enable the OpenSearch query exporter
  enabled: false

  image:
    # -- Image registry for the query exporter
    registry: ghcr.io
    # -- Image repository for the query exporter
    repository: cloudoperators/opensearch-query-exporter
    # -- Image tag for the query exporter
    tag: v0.1.0
  # -- Image pull policy
  imagePullPolicy: IfNotPresent

  # -- Skip TLS verification when connecting to OpenSearch.
  insecure: false

  # -- Number of replicas
  replicas: 1

  # -- Port the exporter listens on
  port: 9206

  # -- Log level (debug, info, warn, error)
  logLevel: info

  # -- Query timeout
  timeout: 30s

  # -- Maximum allowed time range for queries (rejects queries looking back further).
  # Prevents queries from hitting warm/cold storage tiers.
  maxQueryRange: 168h

  # -- Credentials key from cluster.usersCredentials to use for OpenSearch access.
  # The referenced key must exist under cluster.usersCredentials and will be mounted
  # from the corresponding <key>-credentials Secret.
  credentialsRef: logs

  # -- Additional credential keys for failover (optional).
  # Listed keys are tried in order after credentialsRef.
  additionalCredentialsRefs: []
    # - logs2

  # -- Resource requests and limits
  # @default -- <pre>requests:<br>  cpu: 50m<br>  memory: 64Mi<br>limits:<br>  cpu: 200m<br>  memory: 128Mi</pre>
  resources:
    requests:
      cpu: 50m
      memory: 64Mi
    limits:
      cpu: 200m
      memory: 128Mi

  # -- Pod annotations
  podAnnotations: {}

  # -- Node selector
  nodeSelector: {}

  # -- Tolerations
  tolerations: []

  # -- Security context for the container
  # @default -- <pre>allowPrivilegeEscalation: false<br>readOnlyRootFilesystem: true<br>runAsNonRoot: true<br>runAsUser: 1000<br>capabilities:<br>  drop: [ALL]</pre>
  securityContext:
    allowPrivilegeEscalation: false
    readOnlyRootFilesystem: true
    runAsNonRoot: true
    runAsUser: 1000
    capabilities:
      drop:
        - ALL

  # ServiceMonitor configuration for Prometheus scraping
  serviceMonitor:
    # -- Create a ServiceMonitor for the query exporter
    enabled: true
    # -- Scrape interval
    interval: 30s
    # -- Scrape timeout
    scrapeTimeout: 25s
    # -- Additional labels for the ServiceMonitor (e.g. for Prometheus selector matching)
    labels: {}

  # -- Query configuration. Each entry defines a periodic OpenSearch query
  # whose results are exposed as Prometheus metrics.
  # See https://github.com/SAP-cloud-infrastructure/opensearch-query-exporter for full docs.
  queries: []
    # - name: error_count
    #   support_group: observability
    #   service: logs
    #   interval: 60s
    #   indices: "logs-*"
    #   on_error: preserve
    #   on_missing: drop
    #   query:
    #     size: 0
    #     query:
    #       bool:
    #         must:
    #           - match: { level: "ERROR" }
    #           - range: { "@timestamp": { gte: "now-5m" } }

testFramework:
  # -- Activates the Helm chart testing framework.
  enabled: true
  image:
    # -- Defines the image registry for the test framework.
    registry: ghcr.io
    # -- Defines the image repository for the test framework.
    repository: cloudoperators/greenhouse-extensions-integration-test
    # -- Defines the image tag for the test framework.
    tag: main
  # -- Defines the image pull policy for the test framework.
  imagePullPolicy: IfNotPresent
