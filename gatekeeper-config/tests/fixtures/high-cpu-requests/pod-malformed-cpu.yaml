apiVersion: v1
kind: Pod
metadata:
  name: dummy
  namespace: default
  labels:
    greenhouse.sap/owned-by: my-team
spec:
  containers:
  - name: dummy
    image: alpine:3
    args: ["sleep", "86400"]
    resources:
      requests:
        cpu: "500xyz"
        memory: 2Gi
