apiVersion: apps/v1
kind: ReplicaSet
metadata:
  name: dummy
  namespace: default
spec:
  template:
    metadata:
      labels:
        marker: "pod in replicaset"
    spec:
      containers:
      - name: alpine
        image: alpine:3
