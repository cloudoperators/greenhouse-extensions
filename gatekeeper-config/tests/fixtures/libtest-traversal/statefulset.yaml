apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: dummy
  namespace: default
spec:
  template:
    metadata:
      labels:
        marker: "pod in statefulset"
    spec:
      containers:
      - name: alpine
        image: alpine:3
