apiVersion: batch/v1
kind: Job
metadata:
  name: dummy
  namespace: default
spec:
  template:
    metadata:
      labels:
        marker: "pod in job"
    spec:
      containers:
      - name: alpine
        image: alpine:3
