{{/* 
SPDX-FileCopyrightText: 2024 SAP SE or an SAP affiliate company and Greenhouse contributors
SPDX-License-Identifier: Apache-2.0
*/}}

{{- if .Values.installInstrumentation }}
  {{- range $path, $bytes := .Files.Glob "crds/crd-opentelemetryinstrumentation.yaml" }}
    {{ $.Files.Get $path }}
  {{- end }}
{{- end }}

{{- if .Values.installCollector }}
  {{- range $path, $bytes := .Files.Glob "crds/crd-opentelemetrycollector.yaml" }}
    {{ $.Files.Get $path }}
  {{- end }}
{{- end }}

{{- if .Values.installOpAmpBridges }}
  {{- range $path, $bytes := .Files.Glob "crds/crd-opentelemetry.io_opampbridges.yaml" }}
    {{ $.Files.Get $path }}
  {{- end }}
{{- end }}

{{- if .Values.installSmon }}
  {{- range $path, $bytes := .Files.Glob "crds/crd-smon.yaml" }}
    {{ $.Files.Get $path }}
  {{- end }}
{{- end }}