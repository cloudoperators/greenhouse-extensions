apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: kubeconfig-generator
rules:
- apiGroups: ["greenhouse.sap"]
  resources:
  - organizations
  verbs:
  - get
  - list
  - watch
