apiVersion: v1
kind: Secret
metadata:
  name: kubeconfig-generator
  namespace: {{ .Release.Namespace }}
type: Opaque
data:
  {{ range $org, $cfg := .Values.config -}}
  SWIFT_PASSWORD_{{- $org | upper -}}: {{ required "no password given" $cfg.password | b64enc | quote }}
  {{ end -}}
