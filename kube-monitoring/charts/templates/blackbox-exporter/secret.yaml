{{- if .Values.blackboxExporter.enabled }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ $.Release.Name }}-blackbox-exporter
type: Opaque
data:
  blackbox.yaml: |
    {{ .Files.Get "blackbox-exporter/blackbox.yaml" | b64enc }}
{{- end }}
