apiVersion: v1
kind: Pod
metadata:
  name: "test-kube-monitoring-kube-state-metrics"
  labels:
    type: integration-test
  annotations:
    "helm.sh/hook": test
  namespace: default
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['kube-monitoring-kube-state-metrics.default.svc.cluster.local:8080']
  restartPolicy: Never