apiVersion: v1
kind: Pod
metadata:
  name: "test-kube-monitoring-node-exporter"
  labels:
    type: integration-test
  annotations:
    "helm.sh/hook": test
  namespace: default
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['kube-monitoring-node-exporter.default.svc.cluster.local:9100']
  restartPolicy: Never