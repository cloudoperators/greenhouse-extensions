apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: {{ include "service-proxy.fullname" . }}
rules:
- apiGroups: [""]
  resources: ["secrets"]
  verbs: ["get", "watch", "list"]
- apiGroups: ["greenhouse.sap"]
  resources: ["pluginconfigs"]
  verbs: ["get", "watch", "list"]
