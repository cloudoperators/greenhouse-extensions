{{/*
SPDX-FileCopyrightText: 2026 SAP SE or an SAP affiliate company and Greenhouse contributors
SPDX-License-Identifier: Apache-2.0
*/}}

{{- if and (.Capabilities.APIVersions.Has "monitoring.coreos.com/v1") .Values.openTelemetry.ingestCollector.enabled .Values.openTelemetry.ingestCollector.prometheus.podMonitor.enabled -}}
{{- $root := . -}}
{{- range $name, $cfg := .Values.openTelemetry.ingestCollector.collectors -}}
{{- if and $cfg.enabled (gt (len (default (dict) $cfg.streams)) 0) }}
---
apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: opentelemetry-collector-ingest-{{ $name }}
  labels:
    {{- include "plugin.labels" $root | nindent 4 }}
    {{- if $root.Values.openTelemetry.customLabels }}
    {{ toYaml $root.Values.openTelemetry.customLabels | nindent 4 }}
    {{- end }}
    {{- include "plugin.prometheusLabels" $root | nindent 4 }}
spec:
  podMetricsEndpoints:
    - interval: 60s
      scrapeTimeout: 50s
      path: /metrics
      scheme: http
      port: prometheus
  selector:
    matchLabels:
      app.kubernetes.io/component: opentelemetry-collector
      app.kubernetes.io/instance: {{ $root.Release.Namespace }}.ingest-{{ $name }}
      app.kubernetes.io/managed-by: opentelemetry-operator
      app.kubernetes.io/part-of: opentelemetry
{{- end }}
{{- end }}
{{- end }}
